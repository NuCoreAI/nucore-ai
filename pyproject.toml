[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nucore-ai"
version = "1.3.0"
description = "Lightweight library for command/control/optimization/automation of smart home devices using LLMs"
readme = "README.md"
license = "MIT"
authors = [
 { name="Michel Kohanim", email="michel@universal-devices.com" },
]
dependencies = [
 "requests",
 "openai",
 "anthropic",
 "httpx",
 "pytest-asyncio",
 "websockets",
 "udi_interface",
 "PyYAML",
 "python-dotenv",
]
urls = { "Homepage" = "https://github.com/NuCoreAI/nucore-ai.git" }

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["rag*", "nucore*", "iox*", "unified*", "utils*"]
exclude = ["tests*", "*.tests", "*.tests.*"]

[tool.setuptools.package-data]
unified = ["prompt/*.md", "tools/*.json", "runtime_config.example.json"]
