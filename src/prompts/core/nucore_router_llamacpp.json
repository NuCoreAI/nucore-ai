{
  "type": "function",
  "function": {
    "name": "nucore_router_tool",
    "description": "Results from searching the device database for relevant devices based on user query",
    "parameters": {
      "type": "object",
      "required": ["intent","devices"],
      "properties": {
        "intent": {
          "type": "string",
          "enum": [
            "command_control",
            "routine_automation",
            "real_time_status"
          ],
          "description": "Primary intent of the user query: command_control (immediate device actions), routine_automation (scheduled/conditional logic), real_time_status (query current state)"
        },
        "devices": {
          "type": "array",
          "description": "Analyze user query and map to relevant devices under DEVICE DATABASE and return matched devices ranked by relevance score",
          "items": {
            "type": "object",
            "required": ["device_id", "score"],
            "properties": {
              "device_id": {
                "type": "string",
                "description": "The unique device identifier"
              },
              "score": {
                "type": "number",
                "description": "Relevance score (higher is better)"
              }
            }
          }
        }
      }
    }
  }
}
