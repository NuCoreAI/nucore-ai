{
  "name": "get_property",
  "description": "Read a device's current property value (status, temperature, brightness, etc.).",
  "input_schema": {
    "type": "object",
    "required": ["device_id", "property"],
    "properties": {
      "device_id": {
        "type": "string",
        "description": "The device's real id, exactly as shown for it in DEVICE DATABASE."
      },
      "property": {
        "type": "string",
        "description": "The property's exact display name, exactly as shown for this device in DEVICE DATABASE -- not an id."
      }
    }
  }
}
