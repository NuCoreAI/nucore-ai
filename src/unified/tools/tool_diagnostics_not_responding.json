{
  "name": "diagnostics_not_responding",
  "description": "Investigate a complaint that the customer can't control/reach a device, or nothing happens when they try to control it from NuCore (app, voice, a routine, etc.) -- the NuCore -> device direction. Runs the full mandatory investigation (core services, protocol enabled/connected, then a live status check on the device) in one call, in the correct order, and returns a diagnosis plus a recommended fix when it finds one. Prefer this over manually replaying get_full_system_config/get_core_services_status/send_command yourself for a not-responding complaint -- this can't be skipped or misordered the way a manual sequence can. If the complaint doesn't clearly describe operating a device physically and waiting for NuCore to reflect it, this is the default reading of a vague 'can't communicate with my devices'-style complaint -- use diagnostics_no_status_feedback instead only when the customer explicitly describes a physical/local action not showing up in NuCore.",
  "input_schema": {
    "type": "object",
    "required": ["protocol"],
    "properties": {
      "protocol": {
        "type": "string",
        "enum": ["insteon", "zwave", "zigbee", "matter"],
        "description": "The device's real protocol family, lowercase -- from get_device_family (INSTEON -> insteon, Z-Wave -> zwave, Zigbee -> zigbee, Matter -> matter), never guessed from the device's name. Only insteon has real per-device automated diagnosis today; zwave/zigbee/matter only check whether the subsystem itself is enabled."
      },
      "device_id": {
        "type": ["string", "null"],
        "description": "The customer-named device's real id, exactly as shown in DEVICE DATABASE, if they named one. Optional even after system-level checks pass -- for insteon, omitting it samples 1-2 representative devices automatically rather than requiring you to pick one. For zwave/zigbee/matter, omit it: only the subsystem-level check exists, and a device_id is rejected."
      },
      "force": {
        "type": "boolean",
        "description": "Default false -- leave it out for a normal first check. Set true only when the customer explicitly insists this device isn't responding even after this tool already reported it as responding -- force=true skips treating that success as conclusive and keeps investigating the device's link tables for a real root cause. Never set this on a first call, and never set it just because you personally doubt an earlier result."
      }
    }
  }
}
